import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'strategy-design-pattern',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
